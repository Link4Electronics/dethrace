# reccmp report

This directory stores the reccmp verification report generated by the
`reccmp` job in `.github/workflows/workflow.yaml`.

- `progress.svg` — the status badge shown on the README.
- `report.json` — the machine-readable report, used as the diff baseline for
  the next run.

These files are generated by CI and committed automatically; do not edit them
by hand.